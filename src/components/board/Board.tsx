import React from 'react'
import Bounded from '../Bounded'
import { Input } from './Input';

export const Board = () => {

    return (
        <Bounded>            
            <Input />
        </Bounded>
    )
}
